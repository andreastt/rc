;; Window size
(setq default-frame-alist '(
                            (font . "6x13")
                            (width . 102)
                            (height . 50)
                            ))

;; Show scroll bar on the left side
(scroll-bar-mode 1)
(set-scroll-bar-mode 'left)

(provide 'linux)
