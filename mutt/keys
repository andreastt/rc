# vi(1) like movement
bind pager j next-entry
bind pager k previous-entry
bind attach,index,pager \CD next-page
bind attach,index,pager \CU previous-page
bind pager g top
bind pager G bottom
bind attach,index g first-entry
bind attach,index G last-entry

# ,<shorthand> to jump
# .<shorthand> to save
# folders: (i)nbox, (a)rchive, (t)odo, (g)it
macro index ,i "<change-folder>=INBOX<enter>" "Jump to inbox"
macro index .i "<save-message>=INBOX<enter>" "Save to inbox"
macro index ,a "<change-folder>=Archive<enter>" "Jump to archive"
macro index .a "<save-message>=Archive<enter>" "Save to archive"
macro index ,t "<change-folder>=todo<enter>" "Jump to todo list"
macro index .t "<save-message>=todo<enter>" "Save to todo list"
macro index ,g "<change-folder>=git<enter>" "Jump to git mailbox"
macro index .g "<save-message>=git<enter>" "Save to git mailbox"

# r to reply, R to reply to all
bind index,pager R group-reply
