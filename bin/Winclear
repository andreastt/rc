#!/bin/bash

# Mac OS tries to Unix compliant and cuts the -n argument to echo
# unless we explicitly use another shell emulator than /bin/sh.
echo -n , | 9p write acme/$winid/addr
9p write acme/$winid/data </dev/null
