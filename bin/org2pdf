#!/bin/sh
source=${1%.*}
target=${source:-$TMPDIR$(mktemp XXXXXXXX)}.pdf
PATH=/Library/TeX/texbin:$PATH
pandoc \
	--from org \
	--to pdf \
	--output $target \
	--variable 'fontfamily:fourier' \
	${1:-/dev/stdin}
open $target
