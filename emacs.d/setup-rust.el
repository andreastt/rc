;; rust-mode.el does autoloading and mode mappings internally.  This
;; statement is only an explicit require since we can't trust
;; everything in emacs.d/vendor.
(require 'rust-mode)

(provide 'setup-rust)
