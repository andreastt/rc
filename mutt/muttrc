set use_from = yes
set envelope_from = yes
set user_agent = no

set mbox_type = Maildir
set timeout = 3
set mail_check = 0
set delete
set folder = $HOME/mail/
set spoolfile = +INBOX
set record = +Sent
set postponed = +Drafts
unset mark_old
mailboxes +INBOX

source $XDG_CONFIG_HOME/mutt/host/$HOSTNAME

set header_cache = $XDG_CACHE_HOME/mutt/headers
set message_cachedir = $XDG_CACHE_HOME/mutt/bodies
set alias_file= $XDG_DATA_HOME/mutt/aliases
source $alias_file

source $XDG_CONFIG_HOME/mutt/headers
set mailcap_path = $XDG_CONFIG_HOME/mutt/mailcap
auto_view text/html application/text application/pdf
alternative_order text/plain text text/enriched text/html

set sort = threads
set sort_aux = last-date-received
set attribution = "Also sprach %n:\n"
set forward_attribution_intro = "--------- >8 ---------"
unset forward_attribution_trailer
set sort_alias = alias
set reverse_alias = yes

set copy
set delete
set edit_headers
set fast_reply
set forward_quote
set include
set quit
unset beep_new
unset confirmappend
unset mark_old
unset move

set send_charset = "us-ascii:utf-8"

source $XDG_CONFIG_HOME/mutt/keys
source $XDG_CONFIG_HOME/mutt/lists
source $XDG_CONFIG_HOME/mutt/look
