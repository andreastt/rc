#!/bin/sh

export TERM=dumb
export SHELL=rc
export PAGER=nobs
if [ -d $NAMESPACE ]
then
	export NAMESPACE=$(mktemp -d)
else
	mkdir $NAMESPACE
fi

echo $NAMESPACE

trap "rm -rf $NAMESPACE" 0 1 2 3 15

plumber
acme -a $*

