[user]
	name = Andreas Tolfsen
	email = ato@sny.no

[branch]
	autosetuprebase = always

[push]
	default = current

[fetch]
	recurseSubmodules = true

[pull]
	rebase = false

[merge]
	ff = false
	log = true

[alias]
	st = status
	co = checkout
	cp = cherry-pick
	serve = instaweb

	lo = log --oneline
	fixup = !git commit --fixup=`git log --format='%H %s' | grep -Ev '[a-z0-9]{40}\\sfixup!\\s' | head -n1 | cut -f 1 -d ' '`

	current-branch = rev-parse --abbrev-ref HEAD
	fork-point = !git merge-base $(git current-branch) central
	histedit = !git rebase -i $(git fork-point) --autosquash

[cinnabar]
	helper = $HOME/src/git-cinnabar/git-cinnabar-helper
